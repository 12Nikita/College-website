//Q10.Validate whether a given value type is null or not:

function isNull(value) {
  return value === null;
}

console.log(isNull(null)); // Output: true
console.log(isNull(undefined)); // Output: false