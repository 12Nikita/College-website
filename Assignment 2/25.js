/* Q25.Redirect to a specified URL: */

function redirectTo(url) {
  window.location.href = url;
}

// Example usage
redirectTo('https://www.example.com');

//Output:This code will immediately redirect to the specified URL.